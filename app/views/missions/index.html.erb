<div class="container-fluid">
  <div class="row">
    <%= render 'layouts/sidebar' %>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 py-3 px-4">
      <%= render 'layouts/notice' %>
      <div class="container">
        <h2>任務牆</h2>
        <div class="row">
         <%= render partial:"shared/mission_card" , locals: { missions: @missions } %>
        </div>
        <%= paginate @missions %>
      </div>
    </main>
  </div>
</div>

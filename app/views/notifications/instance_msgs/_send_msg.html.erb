<%= link_to instance_path(notification.notifiable.instance), id: notification.id, data:{behavior:'notification-item'} do %>
<%= render partial:"notifications/shared/notification_item", locals:{title:'任務牆有新訊息',content:"#{notification.actor.name}在#{notification.notifiable.instance.mission.name}任務牆上留言：「#{notification.notifiable.content}」", notification: notification, avatar: notification.actor.avatar_url} %>
  <%end%>
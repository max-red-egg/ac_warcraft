<%= link_to invitation_path(notification.notifiable), id: notification.id, data:{behavior:'notification-item'} do %>
  <%= render partial:"notifications/shared/notification_item", locals:{title:'取消邀請通知',content:"#{notification.actor.name} 取消了 #{notification.notifiable.instance.mission.name}任務", notification: notification, avatar: notification.actor.avatar_url} %>
<%end%>
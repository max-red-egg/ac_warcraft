<%= link_to invitation_path(notification.notifiable), id: notification.id, data:{behavior:'notification-item'} do %>
  <%= render partial:"notifications/shared/notification_item", locals:{title:'新的邀請',content:"#{notification.actor.name} 邀請你組隊參加 #{notification.notifiable.instance.mission.name}任務", notification: notification, user: notification.actor} %>
<%end%>

<%= link_to invitation_path(notification.notifiable), id: notification.id, data:{behavior:'notification-item'} do %>
  <%= render partial:"notifications/shared/notification_item", locals:{title:'邀請拒絕通知',content:"#{notification.actor.name} 拒絕了你邀請的 #{notification.notifiable.instance.mission.name}任務", notification: notification, avatar: notification.actor.avatar_url} %>
<%end%>